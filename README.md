# Block2HW4

## Задача 22
```
решена с помощью приведений списков ко множествам и последующим использованием функций функции sorted() и ().union()
```
## Задача 24
```
решена:
1 Этап:
проверка - если количество кустов больше, либо равно, количеству кустов, которое может обрабатать модуль, то возвращается сумма ягод с данных кустов без последующей проверки
2 Этап:
если количество кустов больше, чем возможности модуля, то сравниваем первый (нулевой) элемент с элементом под индексом размерностью на один больше, чем возможности модуля. И проверяем до конца цикла.
С помощью circular_index, когда размер индекса модуля будет превышать размер массива, сработает trigger, отнимет размерность массива и вернет индекс к валидной индексации проверки, например:
modul = 2
i = 0
len_spisok = 5
                      i = 0 1 2 3 4
                  modul = 2 3 4 0 1
                              trigger = true(1) 
```